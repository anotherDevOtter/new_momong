# 2026-06-17 배포 과정

3WAY 컨설팅 저장 결함(B1·B3) 수정 + 얼굴분석 표 매핑(항목1) + 리포트 코스별 분기.
배포 대상: **`origin/main`(`f555426`) 이후 미push 커밋 전부** (개수는 아래 `git log origin/main..HEAD` 로 확인).
(P1~P4 동적구조 작업은 별도 브랜치 `feat/face-analysis-dynamic` 라 **이번 배포에 안 섞임**.)

## 변경점
- **B1**: 컨설팅 단계 입력(퍼스널컬러·골격·이미지방향·헤어디자인·모질)이 이제 다 저장됨 (이전엔 누락)
- **B3**: 컨설팅↔고객 연결을 전화번호 → customerId 로 전환 → **전화번호 없는 고객도 저장·조회됨**
- **항목1**: 얼굴분석 결과 표(WNC/SNH) 라벨·모듈 매핑 수정 — '코 폭'이 죽은 모듈에 묶여 영구 중립이던 버그 등 해소
- **리포트**: 코스별 노출 페이지 분기 + 얼굴분석값 연동
- **admin**: FaceOverlay 기본선 흰색
- **문서**: 얼굴분석 매핑/디자이너 폼/동적구조 설계 추가

## ⚠️ 주의점
1. **백엔드 먼저 배포** (저장 로직이 신 백엔드 필요). 프론트만 먼저 나가면 전화 없는 고객 저장 시 중복 고객 생김.
2. **DB 작업 없음** — 마이그레이션 불필요, 백필 이미 완료.
3. **로그인 테스트 계정 변경은 push 안 함** (로컬만).
4. Python·환경변수 변경 없음.

## 미push 커밋 배포 방법

이 저장소는 **`main` 에 push = 자동 배포** (GitHub Actions = 백엔드 EB / Amplify = 프론트 user·admin).
아래 9개 커밋을 한 번의 push 로 내보낸다.

**0) 배포 전 확인**
```
git checkout main
git log --oneline origin/main..HEAD     # 미push 커밋 목록 확인 (f555426 이후 전부)
git status --short                       # 로그인 2개는 '커밋 안 됨' 상태 → push 에 자동 제외됨 (의도된 것)
```
- DB 마이그레이션: **없음** (백필 이미 완료). 추가 SQL 실행 불필요.
- 환경변수·Python 서버: 변경 없음.

**1) push (자동 배포 트리거)**
```
git push origin main
```

**2) 백엔드 먼저 그린 확인** ⚠️ (B3 저장 로직이 신 백엔드 필요)
- GitHub Actions backend deploy 성공: https://github.com/anotherDevOtter/new_momong/actions
- `/api/docs` 접근 OK

**3) 프론트 빌드 확인**
- Amplify user / admin 빌드 성공 (콘솔)

> 한 번의 push 지만, **백엔드 deploy 가 프론트보다 먼저 그린**인지 보고 넘어갈 것.
> (프론트가 먼저 끝나도 큰 문제는 없으나, 전화 없는 고객 저장은 백엔드 반영 후 정상)

## 배포 후 확인
- 3WAY 끝까지 1회 → 저장 확인
- **전화번호 없는 고객** 생성 → 끝까지 → customer_id 채워지는지 (B3 핵심)
- 고객 상세 컨설팅 이력 표시

```sql
SELECT id, customer_id, client_info->>'phone' AS phone, created_at
FROM consultations ORDER BY created_at DESC LIMIT 5;
```

## 롤백
`git revert <commit>` + push (백엔드 먼저). DB 롤백 대상 없음.
