# `new` 코스 테스트 시나리오

> 시안 이식본(`?course=new`) 확인용. 조건이 갈리는 지점만 모았다.
> 2026-09-02 갱신 · 로컬 전용 (`localhost`)

## 준비

| 서버 | 주소 |
|---|---|
| 유저 | http://localhost:3203 |
| 어드민 | http://localhost:3202 |
| 백엔드 | http://localhost:3200 |
| 파이썬(얼굴분석) | http://localhost:3201 |
| 시안 원본 | http://localhost:5273 |

**진입** — `localhost:3203` → 테스트 계정 로그인 → FIT 헤어컨설팅 → 고객 선택 →
코스 카드 **`1WAY 헤어컨설팅 (신규 이식) · DEV`**

**주소로 바로 열기**
```
http://localhost:3203/3way/consulting?course=new&customerId=<UUID>&step=<화면>
```
`step` : `summary` `faceAnalysis` `aiFaceFeature` `aiFaceResultDerived`
`hairConsulting` `hairDesign` `hairTexture` `nextDirection` `preSurveyReview`

**테스트용 고객** — 고객은 계정(디자이너)별로 갈린다. 아래는 전부
테스트 계정(`another.dev.otter@gmail.com`) 소유라 로그인하면 목록에 보인다.

| 이름 | 성별 | UUID | 사전설문 |
|---|:--:|---|:--:|
| 테스터 | 여 | `a1d81756-ce2d-4460-a857-e3b640622294` | 2건 |
| 테스터남 | 남 | `196ac9e2-1ec0-4d34-b3e0-ea0b5c7f1322` | 0건 |
| 사전설문없음 | 여 | `73a209c2-3ded-44c1-8a9d-0e71ea28f8a0` | 0건 |
| P4테스트 | - | `2dacc276-5789-4ed9-811c-6bcebe80b6bb` | 1건 |

> `조승원`·`조승원2`·`조승원3`·`조승원4` 는 **다른 계정(`test@local.dev`) 소유**라
> 테스트 계정으로 로그인하면 목록에 안 보인다.

---

## S1. 흐름 · 진행률

| # | 조건 | 확인할 것 |
|:--:|---|---|
| 1-1 | 사전설문 **있는** 고객 | `STEP 1 / 8` 로 시작, 첫 화면이 **컨설팅 전 정보 요약** |
| 1-2 | 사전설문 **없는** 고객 | 요약 화면이 **아예 안 나오고** 얼굴 촬영부터. 총 단계가 하나 줄어듦 |
| 1-3 | STEP 1 에서 **뒤로** | 컨설팅 시작(고객 확인) 화면으로 나감 |
| 1-3b | 그 시작 화면 제목 | **`1WAY · 신규 컨설팅 시작`** (예전엔 코스와 무관하게 `3WAY 컨설팅 시작` 이었다) |
| 1-4 | 얼굴 촬영 → 분석 중 → 이목구비에서 **뒤로** | 분석 중 화면을 **건너뛰고** 얼굴 촬영으로 (전환 화면은 되돌아가지 않는다) |
| 1-5 | 이목구비에서 조정 → 다음 → **뒤로** | 조정한 값이 **그대로 남아 있다** (예전에는 분석 결과로 되돌아가 날아갔다) |
| 1-6 | 마지막까지 진행 | 순서 = 요약 → 얼굴촬영 → 분석중 → 이목구비 → 최종타입 → 헤어컨설팅 → **헤어디자인 → 헤어질감** → 다음방향 → **[다음] → 리포트 팝업** |
| 1-7 | 다음 방향에서 `다음` | **사전설문 확인 화면 없이 리포트가 바로 뜬다** (시안 흐름) |

## S2. 얼굴 판정 — 측정 / 미측정

| # | 조건 | 확인할 것 |
|:--:|---|---|
| 2-1 | 촬영을 **건너뛰고** `step=hairConsulting` 으로 바로 | 이미지맵에 **조준점·기준선 없음**, 오른쪽 `고유 · 미측정` |
| 2-2 | 〃 상태에서 목표 칸 클릭 | **추구미는 정상 표시**, 화살표 없음, `강조/커버` 대신 *"고유 이미지타입이 없어 …"* |
| 2-3 | 이목구비에서 **형태축만** 조정 | `Warm × 미측정` — 한 축만 판정된다 |
| 2-4 | 형태축 6개를 **Warm 쪽(왼쪽 끝)** 으로 | 최종 이미지타입이 `Warm × …` 로 바뀜 |
| 2-5 | 형태축을 **2:2 동점** 으로 (Warm 2 · Cool 2) | `Neutral` 로 판정 (한쪽으로 단정하지 않는다) |
| 2-6 | 아무것도 안 만지고 최종 이미지타입 화면 | `미측정` · `NOT MEASURED` (예전엔 근거 없이 CHIC 이 떴다) |

## S2-B. 이목구비 집중 분석 — 사진·게이트

| # | 조건 | 확인할 것 |
|:--:|---|---|
| 2b-1 | 값이 없는 항목이 남은 상태 | 버튼이 **`미측정 N개 남음 →`** (회색). 눌러도 안 넘어가고 **남은 첫 항목으로 이동** |
| 2b-2 | `측정 완료` 클릭 | 슬라이더를 안 움직였어도 **지금 위치가 값으로 확정**되고 카운트가 줄어든다 |
| 2b-3 | 20개 다 채움 | 버튼이 **`결과 도출 →`** 로 바뀐다 |
| 2b-4 | 사진 아래 **`분석 ON/OFF`** | 끄면 측정선이 사라지고 맨 얼굴만 보인다 |
| 2b-5 | **`크게 보기`** | 모든 항목에서 열린다 (모듈 없는 항목 포함). 상단 `‹ ›` · 키보드 `← →` 로 **20항목 전체** 이동, `Esc` 로 닫기 |
| 2b-6 | 확대 화면에서 분석 OFF → 닫기 | 작은 화면도 **OFF 상태가 유지**된다 |
| 2b-7 | 측정선 색 | **검정**이고 수치 글자는 안 나온다 |
| 2b-8 | `가이드` 버튼 | 세로 중심선·가로선이 **사진 비율에 맞춰** 그려진다 (실측 도형이 있는 화면에서도 나온다) |

## S3. 헤어컨설팅 — 추천

| # | 조건 | 확인할 것 |
|:--:|---|---|
| 3-1 | 판정 `Warm × Hard` (형태축 Warm, 비율축 Hard) | 길이 탭에 **단발·미디움·롱** 추천, `숏` 은 제외 |
| 3-2 | 〃 | 앞머리 **사이드뱅·잔머리**, 컬감 **웨이브** |
| 3-3 | 판정이 **코너 4칸**(Warm×Soft, Cool×Soft, Warm×Hard, Cool×Hard) | 검은 **`RECOMMENDED`** 뱃지. 축마다 **여러 개**가 붙을 수 있다 (처방이 범위로 나온다) |
| 3-4 | 판정이 **가운데 5칸**(Neutral 이 섞인 칸) | **추천 표시가 하나도 안 뜬다** (문서가 미검증이라고 밝힌 칸) |
| 3-4b | 이미지맵 라벨 | `고유미`·`추구미` 가 각자 칸 위쪽에 있고, **화살표·칸 이름과 겹치지 않는다** (상대가 왼쪽 위면 오른쪽 위로 비킨다) |
| 3-5 | 미측정 상태 | 뱃지가 **하나도 안 뜬다** |
| 3-6 | 모질 탭에서 **강손상(4단계) 이상** 선택 | 컬러 탭 **`현재 상태 유지`** 에 추천 뱃지 |
| 3-7 | 컬러 탭 | 단색이 아니라 **모발 사진 5장** + `COLOR CODE #6B5B4E` 같은 색상값 |
| 3-8 | 스타일 5축을 다 골라야 | **다음 단계** 버튼이 열린다 (`0/5` → `5/5`) |

## S4. 헤어디자인 · 헤어질감

| # | 조건 | 확인할 것 |
|:--:|---|---|
| 4-1 | 화면 진입 직후 | **아무것도 선택돼 있지 않다** (예전엔 쇄골·시스루·C컬·톤다운이 미리 찍혀 있었다) |
| 4-2 | 판정 있음 | 상단 3줄이 실제 값 — `FEMININE · 페미닌` / `이미지 강조` 또는 `중화 · 커버` / `부드러운 · 여성스러운` |
| 4-3 | 추구미를 **인접 칸**으로 선택 | 추천 전략 `이미지 강조` |
| 4-4 | 추구미를 **먼 칸**으로 선택 | 추천 전략 `이미지 중화 · 커버` |
| 4-5 | 추구미 **선택 안 함** | 추천 전략 `—` |
| 4-6 | 판정 없음(미촬영) | 추천 뱃지 없음, 상단 3줄 `—` |
| 4-7 | 같은 옵션 두 번 클릭 | **선택 해제**된다 |
| 4-8 | 헤어질감 화면 | 손상도·모질·굵기·숱 4항목 선택 가능 |

## S5. 다음 방향

| # | 조건 | 확인할 것 |
|:--:|---|---|
| 5-1 | 제목 | `NEXT DIRECTION` 이 **한 번만** 나온다 (두 줄 중복이었음) |
| 5-2 | `Step 12 of 12` | **없어야 한다** (시안 잔재) |
| 5-3 | 이동 방향 2개 선택 | 변화 강도가 자동 계산돼 표시 |

## S6. 리포트 (다음 방향 → 다음)

**10장**이어야 한다. 퍼스널컬러 페이지는 없다.

| # | 페이지 | 확인할 것 |
|:--:|---|---|
| 6-1 | 1p 표지 | 고객명·날짜·디자이너명 |
| 6-2 | **2p 얼굴 구조** | 실측 5항목. 촬영 안 했으면 전부 `미측정` |
| 6-3 | **3p 이미지축** | 점이 판정 좌표에 찍힘 (예: Warm 아래·Hard 오른쪽), `Tone Axis / Balance Axis` 가 실제 값 |
| 6-4 | 3p 미측정 | 점 없이 `미측정` |
| 6-5 | **4p 모질** | 헤어컨설팅에서 고른 손상도·굵기·숱·곱슬. 안 고른 축은 `—` |
| 6-6 | **5p 오늘의 디자인** | 스타일 5축에서 고른 값 (가르마 포함) |
| 6-7 | **6p 이미지 이동** | `고유 페미닌 — 맵에서 N칸 — 추구 프레시한`, 해당 단계만 진하게 + `이번 컨설팅` |
| 6-8 | 6p 추구미 미선택 | `선택 안 함`, 단계 강조 없음 |
| 6-9 | 7p 디자인 사이클 | 다음 방향에서 고른 월별 시술 |
| 6-10 | **8p 다음 방향 요약** | 고른 이동 방향 + 변화 강도 |
| 6-11 | 페이지 번호 | `Page n of 10` 이 인디케이터와 맞는다 |
| 6-12 | **9p 개인 노트** | ⚠ 아직 **고정 문장**이다 (문서 근거 없음 · 확인 대기) |
| 6-13 | 마지막 장(10p) | `다음` 이 아니라 **검은 `완료` 버튼**. 누르면 리포트가 닫히고 **완료 화면**으로 |
| 6-14 | 상단 **PDF 다운로드** 아이콘 | `PDF 만드는 중…` 뒤에 `고객명_이미지설계리포트_YYYY-MM-DD.pdf` 저장 (10장) |
| 6-15 | 완료 화면의 `PDF 저장` | 리포트가 다시 열린다 → 거기서 내려받는다 |

## S7. 완료 · 저장

| # | 조건 | 확인할 것 |
|:--:|---|---|
| 7-1 | 리포트 닫고 완료 화면 | `3WAY` 가 아니라 **`1WAY · 신규`** 로 표기 |
| 7-2 | 저장 확인 | 아래 쿼리로 방금 값이 들어갔는지 |

```bash
cd new_momong/backend
PGPASSWORD=$(grep '^DB_PASSWORD=' .env | cut -d= -f2-) \
psql -h localhost -U postgres -d fit_hair -c \
"SELECT client_info->'threeWay'->'consultData' FROM consultations ORDER BY created_at DESC LIMIT 1;"
```

**들어 있어야 하는 것** — `hairConsulting`(스타일 5축 + 모질 4축 + 목표타입) ·
`faceItemPositions`(이목구비 20항목) · `finalImageType` · `hairDesign` · `hairTexture` ·
`cycleData.directions` · `cycleData.changeLevel` · `newPreInterview`

**들어 있으면 안 되는 것** — 고르지 않은 축이 값으로 채워져 있는 것
(`hairDesign` 은 안 고르면 `null`, `finalImageType` 은 미측정이면 저장 안 됨)

## S8. 기존 코스 회귀 (건드리지 않았는지)

| # | 조건 | 확인할 것 |
|:--:|---|---|
| 8-1 | `?course=1way` | 예전 13화면 그대로, 사전 인터뷰부터 시작 |
| 8-2 | 1way 의 헤어디자인 | 쇄골·시스루·C컬·톤다운이 **기본 선택**돼 있고 고정 추천 뱃지도 그대로 |
| 8-3 | 1way 리포트 | 11장 (퍼스널컬러 포함 여부는 코스 규칙대로) |
