# 확인이 필요한 것들

> `new` 코스(시안 이식본) 작업 중 **문서·시안만으로는 정할 수 없어 보류한 항목**을 모았다.
> 원장·디자이너 확인이 필요한 순서로 정렬했다.
> 2026-09-02 · 관련 문서: `docs/7.14 알고리즘.html` · `docs/8.10헤어맵핑.html`

---

## A. 판정 알고리즘 (7.14)

### A1. 판정에 쓰는 항목이 문서와 다르다 🔴

문서는 축마다 **8항목**인데 우리 화면은 **10항목**이다.

| | 문서 축1 (형태 8항목) | 우리 형태축 (10항목) |
|---|---|---|
| 둘 다 있음 | 페이스라인 · 광대 · 눈 형태 · 눈꼬리 · 코 너비 · 입술 두께 | ✅ |
| **문서에만** | 눈썹 방향 · 눈 앞머리 | 항목은 있으나 **분석 모듈이 없다** |
| **우리에만** | — | **눈썹 형태 · 코 높이** |

축2(비율)도 같다 — 문서에 없는 **쌍꺼풀 · 눈 바깥 여백 · 코 길이**가 우리 판정에 들어가 있다.
특히 **쌍꺼풀은 문서가 *"Layer 1 판정 변수에 포함되지 않는다"* 고 명시**하고 Layer 3 보정자로 분리했다.

**질문** — 판정 항목을 문서의 8항목으로 맞춰야 하나? 아니면 지금 10항목이 현장 기준인가?

### A2. 문서가 요구하는데 우리가 못 재는 항목 🔴

`눈썹 방향` · `눈 앞머리` 는 문서 축1 목록에 있는데 **파이썬 분석에 대응 모듈이 없다.**
지금은 디자이너가 화면에서 직접 잡아야 넘어갈 수 있게 해뒀다.

**질문** — 이 두 항목을 분석 모듈로 추가할 계획이 있나? (얼굴분석 쪽 개발이 필요하다)

### A3. 등급(W1~C8) 을 도입할까 🟠

문서 정의: `등급 = W개수 − C개수` → `1–2 중립 보정 / 3–5 표준 / 6–8 극단 허용`.
문서는 *"헤어 제안 방향·강도에 사용, 타입 확정 계산에는 직접 사용 안 함"* 이라고 못박았다.

지금은 **도입하지 않았다.** 처방을 화면에 크게 보여주지 않기로 해서 쓸 자리가 없었다.

**질문** — 등급을 화면에 보여줄 자리가 필요한가? (예: `이 얼굴은 처방을 과감하게 밀어도 된다`)
도입한다면 우리는 항목 수가 문서와 달라 **1~8 범위를 그대로 못 쓴다** — 측정 항목 수로 환산할지도 정해야 한다.

### A4. Layer 2 · 3 · 4 도입 여부 🟠

| Layer | 내용 | 문서 상태 |
|---|---|---|
| **2** 이미지 벡터 8개 | 핵심변수 7개 → 귀여움·청순함·…·지적임 | 문서가 *"수치는 현재 **가-설계 기준**, 케이스 축적 후 검증·보정 예정"* 이라고 표시 |
| **3** Modifier | 쌍꺼풀 형태로 **벡터만** 보정 (타입 변경 없음) | 정의는 있음 |
| **4** Sensual | 삼백안 **육안 3단계 체크** | **입력 UI 가 우리 앱에 없다** |

셋 다 지금 없다. 최종 이미지타입 화면의 **레이더 5각형**이 원래 Layer 2 벡터가 들어갈 만한 자리다.

**질문** — 가-설계 수치를 그대로 넣어도 되나? Sensual 입력 화면이 필요한가?

### A5. 두 문서의 9칸 이름이 서로 다르다 🟢 (우리는 8.10 을 따랐다)

| 칸 | 7.14 | **8.10 · 우리 코드** |
|---|---|---|
| N·S | FRESH 산뜻한 | **PURE 청초한** |
| C·S | PURE 청초한 | **FRESH 프레시한** |
| W·N | SOFT 부드러운 | **CASUAL 캐주얼** |
| N·H | ELEGANT 우아한 | **CLASSIC 클래식** |

8.10 이 나중 문서이고 우리 맵과 일치해서 그대로 뒀다.

**질문** — 8.10 기준이 맞나? (7.14 를 갱신해야 하는 건 아닌지)

---

## B. 헤어 처방 (8.10 Layer 5)

### B1. 9칸 중 5칸이 미검증이다 🔴

문서가 스스로 표시해뒀다 — 코너 4칸(**CUTE · FRESH · FEMININE · MODERN**)만 헤어 시트 사진으로 확정,
나머지 5칸(**PURE · CASUAL · NATURAL · CHIC · CLASSIC**)은 `규칙 생성 · 사진 미검증`.

지금은 **검증된 4칸에서만 추천을 띄우고, 5칸은 아무 표시도 하지 않는다.**
그런데 문서도 *"실제 케이스 대부분이 중간띠에 들어온다"* 고 적어서, **추천이 안 뜨는 고객이 많을 것**이다.

**질문** — 5칸을 언제 검증할 수 있나? 그때까지 추천 없이 가도 되나?

### B2. 처방 문구를 옵션에 잇는 사전 🟠

문서는 `단발 ~ 긴 머리` 처럼 **범위**로 말하고, 화면은 `숏/단발/미디움/롱` 옵션이라 잇는 표가 필요했다.
문서가 이름을 대놓고 말한 것만 이었다 (그래서 **한 축에 추천이 여러 개** 붙는다).

애매하게 이은 곳:

| 문서 문구 | 우리가 이은 옵션 |
|---|---|
| `단발 정도` (무게축 N) | `단발` 만 |
| `가벼운 앞머리 또는 옆 앞머리` | `시스루뱅 · 처피뱅 · 사이드뱅` (가벼운 = 문서의 C·S 정의를 재사용) |
| `가볍게 움직이는 컬` | `웨이브` |

**질문** — 위 세 줄이 현장 감각과 맞나?

### B3. 컬 추천이 거의 `웨이브` 하나다 🟠

문서의 컬 축은 **`웨이브 ↔ 생머리`** 두 방향뿐인데, 우리 화면 옵션은 `스트레이트·웨이브·C컬·S컬·CS컬` 다섯이다.
`C컬·S컬·CS컬` 은 문서에 아예 안 나와서 추천하지 않는다.

**질문** — C컬/S컬/CS컬 을 처방 축에 어떻게 넣을지 기준이 있나?

### B4. 가르마 · 컬러는 문서 밖이다 🟢

문서 처방 7항목에 없다. 지금은 디자이너가 직접 고른다.
컬러 탭만 **모발 손상도 4단계(강손상) 이상이면 `현재 상태 유지`** 를 권한다 — 근거는 그 화면의 손상도 안내 문구
(*"화학 시술은 최소화" · "절대 금지"*)다.

**질문** — 컬러 추천을 이 기준으로 둬도 되나?

### B5. 처방 4항목을 화면에 안 보여준다 🟢

처방 7항목 중 `실루엣 · 라인 · 무게감 · 텍스쳐` 는 우리 화면에 대응 자리가 없다.
시안에도 그 화면이 없어서 **표시하지 않기로 했다.** 데이터는 코드에 다 들어와 있다.

**질문** — 시술 중에 이 처방을 보고 싶은가? 필요하면 화면을 하나 만들면 된다.

---

## C. 리포트

### C1. 9페이지 `PERSONAL BRAND NOTE` 🔴

문장 4개 + 박스 2줄이 **전부 고정값**이다. 두 문서에 근거가 없다 (`노트`·`브랜드`·`플랜` 0건).

```
"고객님은 구조적으로 균형이 좋은 타입입니다"      ← 누구에게나 이렇게 나온다
"강한 변화를 주기보다 정교한 방향 설정이 …"
```

첫 줄은 **진단처럼 보이는 문장**이라 특히 문제다.

**질문** — 이 페이지를 (A) 판정 결과로 문장을 만들지 (B) 디자이너 메모 칸으로 바꿀지 (C) 뺄지?

### C2. 2페이지에 **못 재는 항목이 2줄** 있다 🟠

시안 항목 5개를 그대로 쓰기로 했다. 값의 출처는 이렇다:

| 항목 | 출처 |
|---|---|
| 얼굴 가로세로 비율 | SNH 2 얼굴 길이 → `1 : 1.29` |
| 상중하 비율 | SNH 11 → 범위(`상안부 우세` / `균형` / `하안부 우세`) |
| 중안부 비율 | SNH 11 → `1 : 1.91` (mm 아님 — C4 참고) |
| **좌우 대칭** | **모듈 없음 → `미측정`** |
| 윤곽 강도 | WNC 2 턱 각도 → 등급만 나와서 `약함` / `중간` / `강함` |

**질문** — `좌우 대칭` 을 분석 모듈로 추가할 수 있나?
참고로 중안부 모듈은 이미 **좌·우 비율을 각각 계산**하고 있어서 (왼쪽 값만 내보낸다) 대칭 판단에 쓸 여지가 있다.

### C4. 길이를 **mm 로 표기할 근거가 없다** 🟠

리포트 2페이지의 `중안부 길이` 를 `32.5mm` 처럼 적으려면 **사진에서 실제 길이(mm)를 알아야 한다.**
그런데 분석이 주는 값은 **비율**이다 — `중안부 세로/가로 = 1.91` 같은 배수라서 mm 로 바꿀 수 없다.
사진에 크기 기준(자·카드 등)이 없으면 픽셀을 mm 로 환산할 방법이 없다.

지금은 항목 이름을 **`중안부 비율`** 로 바꾸고 `1 : 1.91` 로 적는다.

**질문** — mm 표기가 꼭 필요한가?
필요하다면 촬영 방식이 바뀌어야 한다 (얼굴 옆에 크기 기준물을 두거나, 촬영 거리를 고정하거나).

### C3. 퍼스널컬러 페이지를 뺐다 🟢

1WAY 는 퍼스널컬러를 진단하지 않는데 리포트에 피부톤·팔레트가 인쇄되고 있어서 페이지를 제거했다 (11장 → 10장).
기존 1WAY 리포트도 원래 같은 규칙(`showPersonalColor`)을 갖고 있었다.

**질문** — 1WAY 고객에게 퍼스널컬러 페이지가 필요한가?

---

## D. 화면 · 운영

### D1. 헤어컨설팅과 헤어디자인이 같은 축을 두 번 묻는다 🟠

`헤어컨설팅 > 스타일 제안` 은 `앞머리·가르마·길이·컬감·컬러` 를 고르고,
바로 다음 `헤어디자인 제안` 도 `길이·앞머리·컬·컬러` 를 고른다. **저장 필드도 따로 나간다.**

시안 흐름이 그래서 그대로 뒀다.

**질문** — 두 화면에서 각각 고르는 게 맞나? 하나로 합치거나 한쪽을 읽기 전용으로 할 수 있다.

### D2. 모질·디자인을 안 고르고 넘어갈 수 있다 🟠

`헤어질감`(손상도·모질 상태·굵기·숱·곱슬 정도)과 `헤어디자인`(길이·앞머리·컬·컬러)은
**안 골라도 다음으로 넘어간다.** 그러면 리포트에 그 자리가 `—` 로 빈다.

이목구비 분석만 **20항목을 다 채워야 넘어가도록** 막아뒀다.
두 화면은 예전에 기본값이 박혀 있어서 안 고르면 엉뚱한 값이 저장됐는데, 그건 없앴다
(지금은 안 고르면 `null` 로 남는다).

**질문** — 두 화면도 같은 방식으로 막을까?

### D3. 가이드선 위치 🟢

세로 중심선 50% · 가로선 52.6%(시안 비율)로 사진 크기와 무관하게 그린다.
눈 실측 좌표로 눈높이를 정확히 잡을 수도 있다.

**질문** — 지금 비율 고정으로 충분한가?

### D4. 사전설문 확인 화면을 뺐다 🟢

시안 흐름대로 `다음 방향 → 리포트` 로 바꾸면서 마지막의 사전설문 확인 화면을 뺐다.
그 내용은 **첫 화면(`INFO 컨설팅 전 정보 요약`)이 이미 보여준다.**

**질문** — 컨설팅 끝에 사전설문을 한 번 더 확인할 필요가 있나?

---

### D5. 헤어컨설팅과 헤어질감도 모질을 두 번 묻는다 🟠

`헤어컨설팅 > 모질 분석` 탭(손상도·굵기·숱·곱슬)과 `헤어질감` 화면(손상도·모질 상태·굵기·숱·곱슬 정도)이
거의 같은 것을 묻는다. **리포트 4페이지는 시안대로 `헤어질감` 값을 쓴다.**
헤어컨설팅의 모질 탭은 화면 안에서만 쓰이고 리포트에는 안 나간다 (저장은 둘 다 된다).

**질문** — D1(스타일 중복)과 같은 건이다. 두 화면을 어떻게 정리할지 함께 정해야 한다.

## E. 아직 안 만든 것

| | 내용 |
|---|---|
| 공유 링크 | 완료 화면의 `링크 공유` 는 현재 주소를 클립보드에 복사만 한다 |
| PDF | 리포트 10장을 이미지로 캡쳐해 A4 PDF 로 저장한다. **글자가 이미지라 검색·복사는 안 된다** |
| 기존 코스 | `1way`·`2way`·`3way` 는 이번 개편 대상이 아니라 예전 화면 그대로다 |
